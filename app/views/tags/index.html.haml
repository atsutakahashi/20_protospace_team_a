.jumbotron
  .container.text-center
    = image_tag("header_logo.png", alt: "PROTO TYPE - SHARE THE PROTOTYPE", height: "73px")

.filter-nav
  .container
    .row
      %ul.nav.nav-pills.col-lg-11
        %li.btn-default.btn-popular
          = link_to "Popular", popular_prototypes_path, remote:true
        %li.btn-default.btn-newest
          = link_to "Newest", root_path, remote: true
      = link_to "VIEW TAGS", tags_path, class: "btn btn-default col-lg-1"
.container
  .row
    %h2 Tags List
    %ul
      - @tags.each do |tag|
        = link_to "#{tag.tags_name}", tag_path(tag), class: 'btn btn-default'
